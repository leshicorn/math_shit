# Шпаргалка по алгебре: Базовые правила и методы решения

## 1. Арифметический «треугольник» (Перестановки)
Используй эти схемы, чтобы безошибочно выражать одну переменную через другие.

| Если дано... | Как найти **$b$** | Как найти **$c$** |
| :--- | :--- | :--- |
| **$a = b \cdot c$** | $b = a / c$ | $c = a / b$ |
| **$a = b / c$** | $b = a \cdot c$ | $c = b / a$ |
| **$a = b + c$** | $b = a - c$ | $c = a - b$ |
| **$a = b - c$** | $b = a + c$ | $c = b - a$ |

> **Правило креста (пропорция):** Если $\frac{a}{b} = \frac{c}{d}$, то $a \cdot d = b \cdot c$.

---

## 2. Работа с выражениями

### Как выразить переменную (Алгоритм «Изоляция»)
1. Перенеси все слагаемые, не содержащие нужную букву, в правую часть (сменив знак!).
2. Если перед буквой стоит число (коэффициент), раздели на него всё уравнение.

*Пример:* Выразить $x$ из уравнения $3x + 6y = 12$:
1. $3x = 12 - 6y$
2. $x = (12 - 6y) / 3 \Rightarrow x = 4 - 2y$

### Вынесение общего множителя
Если в каждом слагаемом есть одинаковая часть, выносим её за скобку:
* $ab + ac = a(b + c)$
* $5x^2 - 10x = 5x(x - 2)$

---

## 3. Квадратные уравнения
Вид: $ax^2 + bx + c = 0$

1. **Ищем Дискриминант:** $D = b^2 - 4ac$
   * Если $D > 0$ — 2 корня.
   * Если $D = 0$ — 1 корень.
   * Если $D < 0$ — корней нет.
2. **Ищем корни:** $x_{1,2} = \frac{-b \pm \sqrt{D}}{2a}$

---

## 4. Системы уравнений второго порядка

### Метод подстановки (Универсальный)
1. Выбери самое простое уравнение (где нет квадратов) и вырази одну букву через другую (например, $y = ...$).
2. Подставь это выражение во **второе** уравнение вместо этой буквы.
3. Реши полученное уравнение (оно станет обычным квадратным).
4. Найди вторую переменную, подставив результат в пункт №1.

### Метод сложения
Применяй, когда видишь одинаковые или противоположные слагаемые (например, $y$ и $-y$ или $x^2$ и $x^2$):
1. Сложи или вычти уравнения друг из друга, чтобы одна переменная исчезла.
2. Реши уравнение с одной оставшейся буквой.

---

## 5. Формулы сокращенного умножения (ФСУ)
* $(a + b)^2 = a^2 + 2ab + b^2$
* $(a - b)^2 = a^2 - 2ab + b^2$
* $a^2 - b^2 = (a - b)(a + b)$
